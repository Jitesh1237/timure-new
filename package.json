{
  "name": "timure-yatayat",
  "version": "1.0.0",
  "description": "Timure Yatayat - Full Stack Application",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
